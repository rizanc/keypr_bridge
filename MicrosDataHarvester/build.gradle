apply plugin:'application'
mainClassName = 'com.micros.harvester.service.DataHarvesterService'

dependencies {
    // Local dependencies
    compile project(':CloudKeyHTBase')
    compile project(':MicrosPMSAdapter')
    compile project(':MicrosOWSServices')
    compile project(':MicrosOXIJaxb')

    // External dependencies
    compile sun_httpserver
    compile jaxb_api
    compile jaxb_impl
    compile jaxb_xjc
    compile xstream
}
